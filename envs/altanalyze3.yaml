name: altanalyze3
channels:
  - conda-forge
  - bioconda
  - defaults
dependencies:
  - python=3.11
  - pip
  # Core scientific libraries (safest to install via Conda)
  - pandas
  - numpy
  - scipy
  - pysam 
  
  # Install AltAnalyze3 via pip
  - pip:
    - AltAnalyze3